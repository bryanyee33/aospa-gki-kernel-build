Based on commits:
https://github.com/LineageOS/android_kernel_xiaomi_sm8450/commit/aea66fe46a3d796d4f85c5979678055438185cbd
https://github.com/Pzqqt/android_kernel_xiaomi_marble/commit/f4ae922da12b91d0b7a8109ba2851a44c61b0ba6
https://github.com/Pzqqt/android_kernel_xiaomi_marble/commit/3e043c16430a95f6d328f5396c14ec18a52ab10a
https://github.com/Pzqqt/android_kernel_xiaomi_marble/commit/518a896d8c0244e3f106e971373b6931135e422f
https://github.com/Pzqqt/android_kernel_xiaomi_marble/commit/da9332389c00f143a61b7b1d1c15afd45ef1c620

---
 sm8450/arch/arm64/kvm/Makefile                                                            | 4 ++++
 sm8450/drivers/mtd/mtdoops.c                                                              | 4 ++--
 sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_encoder.c                      | 2 +-
 sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_rm.c                           | 2 +-
 sm8450/drivers/input/touchscreen/goodix_9916r/goodix_ts_inspect.c                         | 2 ++
 sm8450/lib/lz4/lz4hc.c                                                                    | 4 ++++
 sm8450-modules/qcom/opensource/video-driver/driver/variant/ar50lt/inc/hfi_buffer_ar50lt.h | 1 +
 sm8450-modules/qcom/opensource/video-driver/driver/variant/iris2/inc/hfi_buffer_iris2.h   | 1 +
 sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/msm_vidc_driver.c             | 10 +++++-----
 sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/venus_hfi_response.c          |  6 +++---
 10 files changed, 24 insertions(+), 12 deletions(-)

diff --git a/sm8450/arch/arm64/kvm/Makefile b/sm8450/arch/arm64/kvm/Makefile
index 749a0986a9e3..2b024670b755 100644
--- a/sm8450/arch/arm64/kvm/Makefile
+++ b/sm8450/arch/arm64/kvm/Makefile
@@ -5,6 +5,10 @@
 
 ccflags-y += -I $(srctree)/$(src)
 
+ifeq ($(shell test $(CONFIG_CLANG_VERSION) -ge 210000 2>/dev/null; echo $$?),0)
+ccflags-y += -Wno-uninitialized-const-pointer
+endif
+
 KVM=../../../virt/kvm
 
 obj-$(CONFIG_KVM) += kvm.o
diff --git a/sm8450/drivers/mtd/mtdoops.c b/sm8450/drivers/mtd/mtdoops.c
index ebe9129d06b4..d41eb0a5e5f4 100644
--- a/sm8450/drivers/mtd/mtdoops.c
+++ b/sm8450/drivers/mtd/mtdoops.c
@@ -412,7 +412,7 @@ static void mtdoops_do_dump(struct kmsg_dumper *dumper,
 	}
 
 	/* Only dump oopses if dump_oops is set */
-	if (reason == KMSG_DUMP_OOPS && !dump_oops)
+	if (reason == MTD_DUMP_OOPS && !dump_oops)
 		return;
 
 	kmsg_dump_get_buffer(dumper, true, cxt->oops_buf + MTDOOPS_HEADER_SIZE,
@@ -449,7 +449,7 @@ static void mtdoops_do_dump(struct kmsg_dumper *dumper,
 		printk(KERN_ERR "mtdoops: read pmsg failed sig = 0x%x \n", p_hdr->sig);
 
 	/* Panics must be written immediately */
-	if (reason == KMSG_DUMP_OOPS || reason == KMSG_DUMP_PANIC) {
+	if (reason == MTD_DUMP_OOPS || reason == MTD_DUMP_PANIC) {
 		mtdoops_write(cxt, 1);
 	} else {
 		/* For other cases, schedule work to write it "nicely" */
diff --git a/sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_encoder.c b/sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_encoder.c
index 42fd514a4b..bba6895cd2 100644
--- a/sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_encoder.c
+++ b/sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_encoder.c
@@ -3114,7 +3114,7 @@ static void sde_encoder_virt_enable(struct drm_encoder *drm_enc)
 	int i, ret = 0;
 	struct sde_connector_state *c_state;
 	struct drm_display_mode *cur_mode = NULL;
-	struct msm_display_mode *msm_mode;
+	struct msm_display_mode *msm_mode = NULL;
 
 	if (!drm_enc || !drm_enc->crtc) {
 		SDE_ERROR("invalid encoder\n");
diff --git a/sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_rm.c b/sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_rm.c
index dd20d770c0..22701a362c 100644
--- a/sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_rm.c
+++ b/sm8450-modules/qcom/opensource/display-drivers/msm/sde/sde_rm.c
@@ -1112,6 +1112,6 @@ static bool _sde_rm_check_lm_and_get_connected_blks(
 		u32 conn_lm_mask)
 {
 	const struct sde_lm_cfg *lm_cfg = to_sde_hw_mixer(lm->hw)->cap;
-	const struct sde_pingpong_cfg *pp_cfg;
+	const struct sde_pingpong_cfg *pp_cfg = NULL;
 	bool ret, is_conn_primary, is_conn_secondary;
 	u32 lm_primary_pref, lm_secondary_pref, cwb_pref, dcwb_pref;
diff --git a/sm8450/drivers/input/touchscreen/goodix_9916r/goodix_ts_inspect.c b/sm8450/drivers/input/touchscreen/goodix_9916r/goodix_ts_inspect.c
index 8dbef583f07a..9dc83ec33b87 100644
--- a/sm8450/drivers/input/touchscreen/goodix_9916r/goodix_ts_inspect.c
+++ b/sm8450/drivers/input/touchscreen/goodix_9916r/goodix_ts_inspect.c
@@ -129,5 +129,7 @@
 #define ABS(val)			((val < 0)? -(val) : val)
+#ifndef MAX
 #define MAX(a, b)			((a > b)? a : b)
+#endif
 
 static bool module_initialized;
 static struct seq_file *g_seq;
diff --git a/sm8450/lib/lz4/lz4hc.c b/sm8450/lib/lz4/lz4hc.c
index ba8efff7dd67..16400a85a971 100644
--- a/sm8450/lib/lz4/lz4hc.c
+++ b/sm8450/lib/lz4/lz4hc.c
@@ -79,6 +79,10 @@ typedef enum { noDictCtx, usingDictCtxHc } dictCtx_directive;
 /*===   Macros   ===*/
+#ifndef MIN
 #define MIN(a,b)   ( (a) < (b) ? (a) : (b) )
+#endif
+#ifndef MAX
 #define MAX(a,b)   ( (a) > (b) ? (a) : (b) )
+#endif
 
 
 /*===   Levels definition   ===*/
diff --git a/sm8450-modules/qcom/opensource/video-driver/driver/variant/ar50lt/inc/hfi_buffer_ar50lt.h b/sm8450-modules/qcom/opensource/video-driver/driver/variant/ar50lt/inc/hfi_buffer_ar50lt.h
index baaebc65b2f6e..b2beb192f0c82 100644
--- a/sm8450-modules/qcom/opensource/video-driver/driver/variant/ar50lt/inc/hfi_buffer_ar50lt.h
+++ b/sm8450-modules/qcom/opensource/video-driver/driver/variant/ar50lt/inc/hfi_buffer_ar50lt.h
@@ -7,6 +7,7 @@
 #define __HFI_BUFFER_AR50LT__
 
 #include <linux/types.h>
+#include <linux/minmax.h>
 #include "hfi_property.h"
 
 typedef u8 HFI_U8;
diff --git a/sm8450-modules/qcom/opensource/video-driver/driver/variant/iris2/inc/hfi_buffer_iris2.h b/sm8450-modules/qcom/opensource/video-driver/driver/variant/iris2/inc/hfi_buffer_iris2.h
index 1331c9bb7001a..eacfa85d9350f 100644
--- a/sm8450-modules/qcom/opensource/video-driver/driver/variant/iris2/inc/hfi_buffer_iris2.h
+++ b/sm8450-modules/qcom/opensource/video-driver/driver/variant/iris2/inc/hfi_buffer_iris2.h
@@ -7,6 +7,7 @@
 #define __HFI_BUFFER_IRIS2__
 
 #include <linux/types.h>
+#include <linux/minmax.h>
 #include "hfi_property.h"
 
 typedef u8 HFI_U8;
diff --git a/sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/msm_vidc_driver.c b/sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/msm_vidc_driver.c
index 8647efe8f91a9..2649c2a854417 100644
--- a/sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/msm_vidc_driver.c
+++ b/sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/msm_vidc_driver.c
@@ -29,7 +29,7 @@
 extern struct msm_vidc_core *g_core;
 
 #define is_odd(val) ((val) % 2 == 1)
-#define in_range(val, min, max) (((min) <= (val)) && ((val) <= (max)))
+#define is_in_range(val, min, max) (((min) <= (val)) && ((val) <= (max)))
 #define COUNT_BITS(a, out) {       \
 	while ((a) >= 1) {          \
 		(out) += (a) & (1); \
@@ -5948,8 +5948,8 @@ static bool msm_vidc_allow_image_encode_session(struct msm_vidc_inst *inst)
 	min_height = capability->cap[FRAME_HEIGHT].min;
 	max_height = capability->cap[FRAME_HEIGHT].max;
 	fmt = &inst->fmts[INPUT_PORT];
-	if (!in_range(fmt->fmt.pix_mp.width, min_width, max_width) ||
-		!in_range(fmt->fmt.pix_mp.height, min_height, max_height)) {
+	if (!is_in_range(fmt->fmt.pix_mp.width, min_width, max_width) ||
+		!is_in_range(fmt->fmt.pix_mp.height, min_height, max_height)) {
 		i_vpr_e(inst, "unsupported wxh [%u x %u], allowed [%u x %u] to [%u x %u]\n",
 			fmt->fmt.pix_mp.width, fmt->fmt.pix_mp.height,
 			min_width, min_height, max_width, max_height);
@@ -6075,8 +6075,8 @@ static int msm_vidc_check_resolution_supported(struct msm_vidc_inst *inst)
 
 	/* check if input width and height is in supported range */
 	if (is_decode_session(inst) || is_encode_session(inst)) {
-		if (!in_range(width, min_width, max_width) ||
-			!in_range(height, min_height, max_height)) {
+		if (!is_in_range(width, min_width, max_width) ||
+			!is_in_range(height, min_height, max_height)) {
 			i_vpr_e(inst,
 				"%s: unsupported input wxh [%u x %u], allowed range: [%u x %u] to [%u x %u]\n",
 				__func__, width, height, min_width,
diff --git a/sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/venus_hfi_response.c b/sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/venus_hfi_response.c
index 9045765400d27..4d23d8943c5fd 100644
--- a/sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/venus_hfi_response.c
+++ b/sm8450-modules/qcom/opensource/video-driver/driver/vidc/src/venus_hfi_response.c
@@ -16,7 +16,7 @@
 #include "msm_vidc_memory.h"
 #include "msm_vidc_fence.h"
 
-#define in_range(range, val) (((range.begin) < (val)) && ((range.end) > (val)))
+#define is_in_range(range, val) (((range.begin) < (val)) && ((range.end) > (val)))
 
 extern struct msm_vidc_core *g_core;
 struct msm_vidc_core_hfi_range {
@@ -1609,7 +1609,7 @@ static int handle_system_response(struct msm_vidc_core *core,
 					goto exit;
 				goto exit;
 			}
-			if (in_range(be[i], packet->type)) {
+			if (is_in_range(be[i], packet->type)) {
 				rc = be[i].handle(core, packet);
 				if (rc)
 					goto exit;
@@ -1655,7 +1655,7 @@ static int __handle_session_response(struct msm_vidc_inst *inst,
 					__func__, packet->type);
 				handle_session_error(inst, packet);
 			}
-			if (in_range(be[i], packet->type)) {
+			if (is_in_range(be[i], packet->type)) {
 				dequeue |= (packet->type == HFI_CMD_BUFFER);
 				rc = be[i].handle(inst, packet);
 				if (rc)
-- 
2.43.0
